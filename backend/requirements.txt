# Python 3.14 兼容版本依赖
# 使用支持 Python 3.14 的最新版本

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
asyncpg==0.30.0

# Cache
redis==5.2.1
hiredis==3.3.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Validation
pydantic==2.10.5
pydantic-settings==2.7.1
email-validator==2.2.0

# Image Processing
# Pillow 12+ 与可选 rembg 更兼容；与 TF/torch 常用组合无冲突
Pillow>=12.1.0,<13.0.0
opencv-python==4.10.0.84

# Machine Learning - 使用 TensorFlow 2.18+ (支持 Python 3.14)
tensorflow==2.18.0

# ── NEW: Fashion AI (CLIP + Diffusers) ──────────────────────────────────────
# CLIP for zero-shot fashion classification (FashionCLIP approach)
transformers>=4.36.0,<5.0.0    # CLIP model support
# 务必加版本上界：否则 pip 会装成 torch 2.11+（CPU 轮），覆盖你在 CUDA 上手动装的 +cu124
torch>=2.6.0,<2.7.0
torchvision>=0.21.0,<0.22.0     # 须与 torch 小版本一致；CUDA 请见 docs/PYTORCH_CUDA_WINDOWS.md
# Stable Diffusion for virtual try-on (SD-VTON approach)
diffusers>=0.25.0              # Stable Diffusion pipelines
accelerate>=0.25.0              # Faster model loading
# Scipy for image processing (used in virtual try-on)
scipy>=1.11.0                  # For morphological operations in masking
opencv-python-headless>=4.9.0  # Lightweight OpenCV (no GUI)
# 可选 rembg：试衣抠图更强。注意 rembg 2.x 常要求 numpy>=2.3，与本文件为 TensorFlow 保留的 numpy<2.1 冲突——请勿与 rembg 同环境强装，或改用独立 venv / 卸载 rembg。

# Scientific Computing
numpy>=1.26.0,<2.1.0
pandas==2.2.3
scikit-learn==1.6.1

# Utilities
python-dotenv==1.0.1
httpx==0.28.1
dashscope>=1.20.0,<2.0.0  # 阿里云百炼 / DashScope（可选：虚拟试衣 wanx）

# Logging
loguru==0.7.3

# Property-Based Testing
hypothesis==6.122.4

# Development
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
black==24.10.0
isort==5.13.2
pylint==3.3.3
mypy==1.14.1
